# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Pipeline data artifacts: corpora/models stay local (re-downloadable),
# but vocab + trained vectors travel with the repo so other machines work.
data/*
!data/vocab
!data/vectors

# Environment
.env
.env.*

# Node / Next.js (later phases)
node_modules/
.next/
out/

# OS
Thumbs.db
.DS_Store

# local session artifact
session.json

*.log
cookies.txt
